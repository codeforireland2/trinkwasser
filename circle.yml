machine:
  node:
    version: 0.10.22
deployment:
  production:
    branch: master
    commands:
      - npm run deploy
dependencies:
  pre:
    - git config user.name "$USER_NAME"
    - git config user.email "$USER_EMAIL"
